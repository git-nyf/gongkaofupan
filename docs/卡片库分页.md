# 卡片库分页

## 页码跳转

卡片库分页栏支持在当前页位置直接输入页码。输入后按 `Enter`，或将焦点移出输入框，即可跳转并同步更新地址栏中的 `page` 参数。

- 按 `Escape` 可放弃尚未提交的页码并恢复当前页。
- 空输入不会跳转，输入框会恢复当前页。
- 小于第 1 页的数值按第 1 页处理；大于总页数的数值按最后一页处理。
- 输入当前页不会重复刷新卡片列表。
- 卡片加载期间页码输入暂时禁用，加载完成后自动恢复。

该功能复用现有卡片筛选和分页请求，不增加服务端接口或数据库结构。
